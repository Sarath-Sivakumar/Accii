import 'package:flutter/material.dart';


class Overview_page extends StatelessWidget {
  const Overview_page({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          "OverView Page here"
        ),
      )
    );
  }
}
